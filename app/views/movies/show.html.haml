%a{data: {route: 'routes.movies'}} Back

%h2{data: {bind: 'movie.title'}}

%table
  %tr
    %th Disc Number
    %td{data: {bind: 'movie.disc_number'}}
  %tr
    %th Watched?
    %td{data: {bind: 'movie.watched | watched_human'}}

%p{data: {bind: 'movie.description'}}
